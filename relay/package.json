{
  "name": "skyblock-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "wrangler types && tsc --noEmit",
    "smoke": "node test/live.mjs",
    "test": "wrangler deploy --dry-run --outdir .test-build && node test/relay.mjs"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "miniflare": "5.20260921.0-alpha",
    "typescript": "5.9.3",
    "wrangler": "4.136.2"
  }
}
